import React from 'react'

/**
 * The Home component describes the view for the homepage of the documentation server.
 *
 * @param props
 * @returns {XML}
 */
export default (props) => {
    return (
        <h2>Home</h2>
    )
}